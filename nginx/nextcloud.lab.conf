upstream nextcloud_cluter_ips{
        server 192.168.1.111:30496;
        server 192.168.1.173:30496;
        server 192.168.1.121:30496;
        server 192.168.1.122:30496;
}

server{
        listen 80;
        server_name nextcloud.lab;

        location / {
                # 2. Point Nginx to the upstream group instead of a single IP
                proxy_pass http://nextcloud_cluter_ips;

                proxy_set_header Host $host;
                proxy_set_header X-Real-IP $remote_addr;
                proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
                proxy_set_header X-Forwarded-Proto $scheme;

                proxy_http_version 1.1;
                proxy_set_header Upgrade $http_upgrade;
                proxy_set_header Connection "upgrade";
    }
}

server{
        listen 443;
        server_name nextcloud.lab;

        location / {
                # 2. Point Nginx to the upstream group instead of a single IP
                proxy_pass http://nextcloud_cluter_ips;

                proxy_set_header Host $host;
                proxy_set_header X-Real-IP $remote_addr;
                proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
                proxy_set_header X-Forwarded-Proto $scheme;

                proxy_http_version 1.1;
                proxy_set_header Upgrade $http_upgrade;
                proxy_set_header Connection "upgrade";
    }
}
