apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: docmost-data-storage
  namespace: docmost
spec:
  storageClassName: docmost-data-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
